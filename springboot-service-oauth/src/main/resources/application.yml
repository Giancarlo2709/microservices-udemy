spring:
  application:
    name: services-oauth
  config:
    import: "optional:configserver:"
    
server:
  port: 9100
  
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
